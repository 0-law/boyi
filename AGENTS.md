# AGENTS.md

## 项目定位

本仓库是“金融投研博弈系统”的早期规则库。

当前阶段只实现一个轻量模块：

- 资讯裁判模块

该模块用于处理 A 股短线投研中的资讯类内容，包括公告、新闻、政策、研报、纪要、复盘、小作文、传闻、自媒体、OCR 文本、AI 摘要、涨停原因、异动原因等。

本仓库当前不是交易系统，不是自动下单系统，不是荐股系统。

---

## 当前核心模块

### 资讯裁判模块

路径：

```text
资讯裁判模块/
```

核心文件：

```text
00_资讯裁判入口.md
01_资讯源分类规则.md
02_证据等级规则.md
03_公告处理规则.md
04_新闻处理规则.md
05_小作文处理规则.md
06_复盘信息处理规则.md
07_研报纪要处理规则.md
08_资讯输出模板.md
09_降级与排雷规则.md
10_资讯裁判执行提示词.md
11_资讯裁判自检清单.md
12_资讯裁判样例输出.md
13_真实资讯测试规则.md
```

---

## 核心判断体系

当前统一使用：

```text
H：证据硬度
L：逻辑合理性
C：环境适配度
W：证伪窗口
I：影响力预期
最终评级：SSS / SS / S / A / B / C / R
```

不得恢复旧版 E0-E5 作为主评级。

如果文档中出现 `E0-E5`，只能作为“旧版已废弃”的历史说明存在。

---

## 关键原则

1. 资讯不能直接等于事实。
2. 必须区分事实、观点、推演、传闻、情绪、风险。
3. 小作文不能当事实，但可以作为资金攻击线索。
4. 真实性低不等于影响力低。
5. 真实性高不等于短线影响力高。
6. 证伪窗口只提高博弈空间，不提高真实性。
7. 不可证伪不等于有价值，必须同时评估逻辑合理性和环境适配度。
8. 最终评级表示短线博弈影响力，不表示事实真实性。
9. 与公告、监管、澄清信息冲突的传闻必须降级。
10. AI 推理不是证据，只能作为辅助整理。
11. OCR 和图片识别文字默认低置信，除非有明确来源。

---

## 时间字段规则

资讯裁判模块统一使用以下时间字段组：

```text
source_time：资讯原始发布时间
source_time_precision：原始发布时间精度
ingest_time：系统获得 / 抓取 / 导入该资讯的时间
analysis_time：AI 实际裁判该资讯的时间
timezone：统一时区
```

正式运行中，短线时段判断优先使用 `ingest_time`。

如果 `source_time` 缺失，写 `null`。
如果 `source_time_precision` 缺失，写 `unknown`。
如果 `ingest_time` 缺失，写 `null`。
如果 `analysis_time` 缺失，写 `null`。
`timezone` 默认写 `Asia/Shanghai`。

不得编造时间。
不得在没有依据时猜测盘前、盘中、盘后。

## 时段属性

```text
盘前
盘中
盘后
非交易日
无法判断
```

## 时效状态

```text
新增资讯
延迟获取
旧闻重抓
盘中异动解释
盘后预期
待盘面验证
已部分兑现
无法判断
```

## 时间影响评级原则

```text
时间不改变 H 证据硬度。
时间主要影响 C 环境适配、W 证伪窗口、I 影响力预期和处理结果。
```

---

## 处理结果统一枚举

所有文件中的处理结果必须统一为以下 9 项，且顺序保持一致：

```text
主攻支撑
弹性支撑
观察
高影响低可信观察
小作文池
需要公告验证
降级
排雷
仅记录
```

边界说明：

```text
“主攻支撑”和“弹性支撑”只表示资讯侧支持，不等于最终交易结论，不得输出买卖建议、目标价或仓位建议。
```

---

## 主体类型规则

资讯裁判必须区分以下主体类型：

- A股上市公司
- 港股上市公司
- 美股上市公司
- 海外公司
- 未上市公司
- 政策/监管机构
- 行业协会/交易所
- 产品/模型/项目
- 行业方向
- 宏观数据
- 未知主体

非 A 股主体不得直接当作 A 股股票。

如果需要映射到 A 股，必须标注“映射链待验证”。

---

## 来源升级限制

原文只写“消息 / 据悉 / 预计 / 传出 / 业内人士表示 / 宣布 / 有望 / 或将 / 可能 / 计划 / 拟”时，不得直接标为公告或 H5。

只有明确出现“公告 / 交易所披露 / 定期报告 / 业绩预告 / 监管文件 / 公司公告原文”，或能回溯正式披露文件时，才可标为公告 / H5。

---

## Markdown 时间元数据规则

Markdown 格式资讯可以继续使用，但每条资讯必须尽量带结构化时间元数据。

统一要求：

```text
ISO 8601 时间格式 + UTC+08 时区
默认时区：Asia/Shanghai
```

标准字段：

```yaml
source_time: 2026-07-03T09:12:00+08:00
source_time_precision: minute
ingest_time: 2026-07-03T09:14:31+08:00
analysis_time: null
timezone: Asia/Shanghai
```

补充要求：

1. `source_time` 表示资讯原始发布时间，缺失时写 `null`。
2. `source_time_precision` 必须使用固定枚举：
   `second / minute / hour / date / daypart_morning / daypart_noon / daypart_afternoon / daypart_night / range / unknown`。
3. `ingest_time` 表示系统获得 / 抓取 / 导入资讯的时间，正式运行时应尽量精确到秒。
4. `analysis_time` 表示 AI 实际执行裁判时间，缺失时写 `null`。
5. `timezone` 默认写 `Asia/Shanghai`。
6. 原文只有日期时，只保留日期，不得强行补时分秒。
7. 原文没有发布时间时，不得猜测具体时间。
8. 时段属性判断优先级固定为：`ingest_time > source_time > 无法判断`。
9. 时间不会改变 H 证据硬度，只影响 C / W / I、时段属性、时效状态和处理结果。

---

## 输出边界

任何 Agent 修改本仓库时，必须遵守：

1. 不得输出买卖建议。
2. 不得输出目标价。
3. 不得输出仓位建议。
4. 不得编造股票代码。
5. 不得编造公司业务。
6. 不得把小作文写成事实。
7. 不得把 AI 推理写成证据。
8. 不得把 H 证据硬度当成最终短线评级。
9. 不得把 W 证伪窗口当成真实性。
10. 不得因为信息不可证伪就无条件提高评级。

---

## 修改规则

### 允许修改

允许修改：

```text
资讯裁判模块/*.md
AGENTS.md
README.md
```

### 谨慎修改

谨慎修改：

```text
02_证据等级规则.md
05_小作文处理规则.md
09_降级与排雷规则.md
10_资讯裁判执行提示词.md
```

修改这些文件时，必须保持 H / L / C / W / I 体系一致。

### 禁止操作

未经明确要求，不得：

1. 新增资金模块。
2. 新增盘面模块。
3. 新增技术分析模块。
4. 新增交易决策模块。
5. 新增自动交易相关内容。
6. 大规模重写目录结构。
7. 把资讯裁判模块改成荐股系统。
8. 删除证伪窗口规则。
9. 删除小作文高影响低可信观察规则。

---

## Markdown 链接规则

所有同目录文件链接必须使用相对路径。

正确示例：

```markdown
[02_证据等级规则.md](./02_证据等级规则.md)
```

禁止使用本地绝对路径：

```text
<local absolute path>
```

---

## 表格一致性规则

所有“资讯裁判表”必须使用统一表头：

```markdown
| source_time | source_time_precision | ingest_time | analysis_time | timezone | 时段属性 | 时效状态 | 来源类型 | 摘要 | 对应方向 | 对应个股 | 主体类型 | 信息性质 | 催化类型 | 影响级别 | H证据硬度 | L逻辑合理性 | C环境适配 | W证伪窗口 | I影响预期 | 最终评级 | 验证点 | 证伪触发点 | 降级风险 | 处理结果 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
```

不得在不同文件中使用不同表头。

---

## 采集端建议字段

未来采集端建议为每条资讯提供：

```json
{
  "source_time": "资讯原始发布时间，若无则为空",
  "source_time_precision": "原始时间精度，缺失时为 unknown",
  "ingest_time": "系统采集/导入时间，正式运行必须提供",
  "analysis_time": "AI 实际裁判时间，可为空",
  "timezone": "默认 Asia/Shanghai",
  "source_name": "来源名称",
  "source_type_hint": "来源类型提示，可为空",
  "source_url": "原文链接，可为空",
  "raw_title": "原始标题",
  "raw_text": "原始正文",
  "collector": "采集器名称，可为空"
}
```

说明：
- `source_time` 是资讯发布的时间。
- `source_time_precision` 用于标记原始时间精度，不能乱补时分秒。
- `ingest_time` 是系统知道这条资讯的时间。
- `analysis_time` 用于分析追溯，不直接决定短线影响力。
- `timezone` 默认统一为 `Asia/Shanghai`。
- 短线系统优先使用 `ingest_time` 作为有效进入系统时间。
- 若 `source_time` 缺失，不得由 AI 猜测。

---

## 每次修改后的自检

每次修改完成后，必须检查：

1. 是否仍存在本地绝对路径残留。
2. 是否仍存在本地项目路径残留。
3. 是否误用 `E0-E5` 作为主评级。
4. 是否破坏 H / L / C / W / I 体系。
5. 是否破坏资讯裁判表表头一致性。
6. 是否破坏处理结果枚举顺序。
7. 是否新增了交易建议、目标价、仓位建议。
8. 是否把小作文写成事实。
9. 是否把证伪窗口写成真实性。
10. 是否遗漏 `source_time / source_time_precision / ingest_time / analysis_time / timezone` 统一时间字段。
11. 是否在没有依据时编造盘前、盘中、盘后或具体时间。

---

## 当前开发原则

当前阶段只做“资讯裁判模块”的稳定化。

下一阶段是否接入资金、盘面、技术分析、候选池，由用户另行明确指示。

在用户没有明确要求前，不要主动扩展模块。
